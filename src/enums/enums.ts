export const TITLE = "Todoeux"

export enum PriorityOptionLabels  {
    DEFAULT="Choose Priority",
    LOW = "Low",
    MODERATE = "Moderate",
    HIGH = "High",
}

export enum PriorityOptionValues  {
    LOW = "low",
    MODERATE = "moderate",
    HIGH = "high",
}

export enum  PriorityOptionColors{
    LOW= "#2192FF",
    MODERATE= "#38E54D",
    HIGH= "#FF1E00",
}

export enum StatusOptionLabels {
    ALL = "All",
    REMAINING = "Remaining",
    COMPLETED = "Completed",
}

export enum StatusOptionValues {
    ALL = "all",
    REMAINING = "remaining",
    COMPLETED = "completed",
}